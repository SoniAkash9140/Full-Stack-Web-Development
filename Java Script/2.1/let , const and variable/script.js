let age =30;
age =31;

const birthYear=1991;
//birthYear=1990; // it will give error because we cannot change the value of constant variable

const job = "developer"; // initialized at the time of declaration
//job="teacher"; // it will give error because we cannot change the value of constant variable
//const job; // it will give error because we have to initialize the constant variable at the time of declaration
//const PI; // it will give error because we have to initialize the constant variable at the time of declaration
//PI=3.1415; // it will give error because we have to initialize the constant variable at the time of declaration   
console.log(age);
console.log(birthYear);


var job1="programmer";
job1="teacher"; // we can change the value of variable declared with var keyword    
console.log(job1);